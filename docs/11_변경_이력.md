# 11. 변경 이력

← [← README로 돌아가기](../README.md)

---

## Phase 0 — 초기 인프라 (2026-04-16)

| 파일 | 변경 |
|---|---|
| `rag/` (전체) | `platform/` → `rag/` 패키지명 변경 (stdlib 충돌 해결) |
| `start.bat` | `PYTHONIOENCODING=utf-8`, `PYTHONUTF8=1` 추가; cwd를 `D:\RAGPlatform`으로 변경; `rag.server.main:app` 으로 수정 |
| `configs/settings.py` | `StorageConfig.path` → `__file__` 기반 절대경로 계산으로 수정 |
| `server/main.py` | StaticFiles `/ui` 마운트, `GET /` 리다이렉트 추가 |
| `server/routes/dataset.py` | 수동 입력 엔드포인트 3개 추가; ExportRequest에 `interaction_ids`, `include_manual` 추가 |
| `dataset/interaction_logger.py` | `manual_entries` 테이블 + `log_manual`, `list_manual`, `delete_manual` 메서드 추가 |
| `server/static/index.html` | 훈련 데이터 관리 웹 UI 신규 생성 (Vanilla JS, 탭 2개) |

---

## 추가 파서 + 압축 처리 (2026-04-XX)

| 파일 | 변경 |
|---|---|
| `ingest/parsers/docx_parser.py` (신규) | python-docx로 단락/표 추출, heading 기준 청킹 |
| `ingest/parsers/pptx_parser.py` (신규) | python-pptx로 슬라이드별 텍스트 추출 (AS_IS) |
| `ingest/dispatcher.py` | DocxParser, PptxParser 등록 |
| `ingest/pipeline.py` | `_is_archive()`, `ingest_archive()`, `_extract()` 추가; ZIP/TAR 자동 추출 + Zip-slip 방어 |
| `server/routes/ingest.py` | IngestResponse에 archive_file_count 필드 추가 |
| `requirements.txt` | python-docx, python-pptx 추가 |

---

## 문서 요약 기능 (2026-04-XX)

| 파일 | 변경 |
|---|---|
| `server/routes/ingest.py` | `POST /v1/ingest/summarize` 신규 엔드포인트 (단일/map-reduce 모드, length/language/model 선택) |
| `server/static/index.html` | 3번째 탭 "문서 요약" 추가 (드롭다운 옵션 + 결과 카드 + 복사/저장 버튼) |

---

## Phase 1 — 가이드 규칙 추출 (2026-05-06)

| 파일 | 변경 |
|---|---|
| `dataset/guide_store.py` (신규) | guides + rules 테이블 관리, 270줄 |
| `llm_router/prompts/__init__.py` (신규) | 프롬프트 패키지 |
| `llm_router/prompts/rule_extraction.py` (신규) | 규칙 추출 LLM 프롬프트 + JSON 파서 |
| `server/routes/guide.py` (신규) | 11개 엔드포인트 + 백그라운드 추출, 400줄 |
| `server/dependencies.py` | `guide_store` 등록 + startup init |
| `server/routes/__init__.py` | `guide_router` 노출 |
| `server/main.py` | guide_router 등록 |
| `server/static/index.html` | 4번째 탭 "가이드 → 훈련 데이터" 추가 + 검수 UI + 편집 모달 |

추가된 엔드포인트:
- POST `/v1/guide/upload`
- GET `/v1/guide/list`
- GET/DELETE `/v1/guide/{guide_id}`
- GET/POST `/v1/guide/{guide_id}/rules`
- PATCH/DELETE `/v1/guide/rules/{rule_id}`
- POST `/v1/guide/rules/{rule_id}/split`
- POST `/v1/guide/rules/merge`
- POST `/v1/guide/{guide_id}/finalize`

---

## Phase 1.1 — 진행 로그 + 안정화 (2026-05-06)

| 파일 | 변경 |
|---|---|
| `server/routes/guide.py` | `_extract_rules_background()` 에 시작/그룹별/완료 로그 추가; 첫 호출 vs 이후 호출 구분; Stale 감지 |

추가된 로그:
```
[guide xxx] 파싱 완료: NN 청크, M 페이지 (X.Xs)
[guide xxx] 추출 시작: N 그룹, 모델=...
[guide xxx] 그룹 i/N 처리 중 (페이지 a-b, c자)
[guide xxx] 그룹 i/N 완료: x/y 규칙 저장 (Xs)
[guide xxx] ✓ 추출 완료: ... 규칙, ... 그룹, X.Xs
```

---

## Phase 2 — Q&A 자동 생성 (2026-05-06)

| 파일 | 변경 |
|---|---|
| `dataset/guide_store.py` | `GeneratedQARecord` dataclass + `generated_qa` 테이블 + 8개 CRUD 메서드 추가 |
| `llm_router/prompts/qa_generation.py` (신규) | Q&A 생성 프롬프트 (50/30/15/5 비율 분배 로직) + JSON 파서 + qa_type 정규화 |
| `server/routes/guide.py` | Q&A 6개 엔드포인트 + `_generate_qa_background()` 함수 추가 |
| `server/routes/dataset.py` | ExportRequest에 `include_interactions`, `include_manual`, `include_guide_qa`, `guide_id` 추가; export 로직에 가이드 Q&A 통합 |
| `dataset/interaction_logger.py` | manual_entries 관련 메서드 복구 (log_manual, list_manual, delete_manual) |
| `server/static/index.html` | "[Q&A 생성 →]" 버튼 추가; Q&A 검수 카드 + 필터 + 일괄 작업 + 편집/생성 모달 추가 |

추가된 엔드포인트:
- POST `/v1/guide/{guide_id}/generate_qa`
- GET `/v1/guide/{guide_id}/qa`
- PATCH `/v1/guide/qa/{qa_id}`
- DELETE `/v1/guide/qa/{qa_id}`
- POST `/v1/guide/qa/bulk`
- POST `/v1/guide/rules/{rule_id}/regenerate_qa`

---

## 모델 설정 변경 이력

| 날짜 | default.yaml | 비고 |
|---|---|---|
| 2026-04-16 | gemma3:4b / gemma3:12b | 초기 |
| 2026-04-XX | gemma4:latest / gemma4:latest | (실제 보유 모델 반영) |
| 2026-05-06 | gemma3:1b / gemma3:1b | 테스트용 (RTX 3050 4GB 환경) |

---

## 향후 계획 (Phase 3 후보)

- LLM-as-Judge 자동 검증 (단어 겹침 → LLM 채점)
- 코드 컴파일 자동 검증 (javac 호출)
- 중복 Q&A 자동 탐지 (instruction 유사도)
- 카테고리별 통계/시각화
- Stale guide 자동 정리 (서버 시작 시)
- 동시성 제어 (asyncio.Semaphore)

자세한 설계는 [9. 미완성 항목](09_이슈_및_미완성.md) 참고.

---

## 다음 읽을 문서

- 🎓 [Phase 1 상세](12_Phase1_가이드_규칙추출.md)
- 🤖 [Phase 2 상세](13_Phase2_QA_생성.md)
- ⚠️ [알려진 이슈](09_이슈_및_미완성.md)
